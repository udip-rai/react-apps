import { StyleFunctionProps } from "@chakra-ui/react";

export const ChattingAppCss = (props: StyleFunctionProps) => ({});

export const ChattingHomeCss = (props: StyleFunctionProps) => ({});

export const ChattingFormCss = (props: StyleFunctionProps) => ({
  form: {
    display: "flex",
    flexDirection: "column",
  },
});
